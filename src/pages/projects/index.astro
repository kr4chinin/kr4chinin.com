---
import { getCollection } from 'astro:content';
import Layout from '~/layouts/Layout.astro';

const projects = (await getCollection('projects'))
	.filter(project => !project.data.draft)
	.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

function formatDate(date: Date) {
	return date.toLocaleDateString('en-us', {
		year: 'numeric',
		month: 'short',
		day: 'numeric',
	});
}
---

<Layout>
	<main class="flex flex-grow">
		<div class="mx-auto flex w-full max-w-3xl flex-col gap-4 px-4 py-6">
			<div class="py-4">
				<a href="/" class="font-bold hover:underline">← Home</a>
			</div>

			<div class="mb-6">
				<h1 class="text-3xl font-bold">Projects</h1>
			</div>

			<i class="text-gray-600">Soon...</i>

			<!-- {
				projects.length === 0 ? (
					<p>No projects yet. Check back soon!</p>
				) : (
					<ul class="flex flex-col gap-6">
						{projects.map(project => (
							<li class="flex flex-col items-start">
								<a href={`/projects/${project.slug}`} class="group block">
									<div class="flex flex-col gap-2">
										<h2 class="text-xl font-bold group-hover:underline">{project.data.title}</h2>
										<p class="text-sm text-gray-600">{formatDate(project.data.date)}</p>
										<p class="text-gray-700">{project.data.description}</p>
										{project.data.demoURL && (
											<div class="flex gap-4 text-sm">
												<a
													href={project.data.demoURL}
													target="_blank"
													rel="noopener noreferrer"
													class="font-semibold text-blue-600 hover:underline"
												>
													Live Demo →
												</a>
											</div>
										)}
									</div>
								</a>
							</li>
						))}
					</ul>
				)
			} -->
		</div>
	</main>
</Layout>
