---
interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a
	class="font-semibold text-blue-500 transition-colors hover:text-blue-600 hover:underline active:text-blue-700"
	href={href}><slot /></a
>
